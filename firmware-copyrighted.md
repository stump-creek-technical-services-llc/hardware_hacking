# firmware-copyrighted

The subdirectory firmware-copyrighted is intended for private use,
to store firmware images that cannot be shared.

`firmware-copyrighted` is in the .gitignore so that we don't commit it accidentally.
