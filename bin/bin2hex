#!/usr/bin/env sh
set -e  # exit immediately if any command fails
set -u  # unset variables are errors
if uname -o | grep "Darwin" >> /dev/null
then
  set -o pipefail  # pipeline fails if any command fails

fi
set -x # show everything

xxd "${1}" > "${1}.hex"
